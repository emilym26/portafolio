---
const { title, description } = Astro.props;
---

---
---
<section id="about" aria-labelledby="about-title" class="about-section">
  <div class="about-container">
    <div class="about-header">
      <h2 id="about-title" class="gradient-text">Sobre mí</h2>
      <p class="subtitle">Diseño. Desarrollo. Adaptable</p>
    </div>

    <div class="about-content">
      <p>
        Soy Emily Barba Valdés, <strong>licenciada en Desarrollo de Software </strong> con
        especial interés en el desarrollo frontend.
        Me apasiona crear interfaces que no solo se vean bien, sino que también sean
        funcionales, accesibles y centradas en el usuario.
      </p>

      <p>
        Disfruto cada etapa del proceso: desde la definición de la idea y el prototipado
        hasta la implementación con tecnologías como React, Figma y
        CSS. Mi objetivo es aportar soluciones visuales claras, coherentes y
        con propósito, uniendo lo técnico y lo creativo en cada proyecto.
      </p>
      <p>
        <a href="files/Diploma-Emily Barba.pdf" target="_blank" class="diploma-button">
          Ver / Descargar mi diploma
        </a>
      </p>
    </div>
  </div>
</section>

<style>
  /* Botón diploma */
.diploma-button {
  display: inline-block;
  margin-top: 1rem;
  padding: 0.65rem 1.25rem;
  color: #6366f1;
  font-weight: 600;
  border-radius: 8px;
  text-decoration: none;
  transition: background 0.3s ease, transform 0.3s ease;
}

.diploma-button:hover {
  background: #6366f1;
  transform: translateY(-2px);
}
/* 🪩 --- Estilo general --- */
.about-section {
  position: relative;
  padding: 6rem 1.5rem;
  background: radial-gradient(circle at top, #f8faff 0%, #ffffff 60%);
  overflow: hidden;
}

/* 🧭 Contenedor centrado */
.about-container {
  max-width: 900px;
  margin: 0 auto;
  text-align: center;
  animation: fadeInUp 1.2s ease-out;
}

/* ✨ Encabezado */
.about-header {
  margin-bottom: 2rem;
}

.gradient-text {
  font-size: 2.75rem;
  font-weight: 800;
  background: linear-gradient(90deg, #6366f1, #8b5cf6);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  letter-spacing: -0.5px;
  animation: slideIn 1.2s ease-out;
}

.subtitle {
  font-size: 1rem;
  color: #6b7280;
  letter-spacing: 2px;
  text-transform: uppercase;
  margin-top: 0.5rem;
}

/* 🧠 Contenido */
.about-content p {
  font-size: 1.125rem;
  color: #374151;
  line-height: 1.85;
  margin-bottom: 1.25rem;
  transition: transform 0.3s ease, opacity 0.3s ease;
}

.about-content strong {
  color: #4f46e5;
}

/* 💫 Hover suave */
.about-content p:hover {
  transform: translateY(-3px);
  opacity: 0.95;
}

/* 🌈 Animaciones */
@keyframes fadeInUp {
  0% { opacity: 0; transform: translateY(20px); }
  100% { opacity: 1; transform: translateY(0); }
}

@keyframes slideIn {
  0% { opacity: 0; transform: translateY(-10px); }
  100% { opacity: 1; transform: translateY(0); }
}

/* 📱 Responsive */
@media (max-width: 768px) {
  .gradient-text {
    font-size: 2rem;
  }
  .about-content p {
    font-size: 1rem;
  }
}
</style>
